[tool.poetry]
name = "mnist-playground"
version = "0.1.0"
description = "A playground for classic and modern machine‑learning techniques on the humble MNIST digits dataset."
authors = ["hamishhuggard <hamishhuggard@gmail.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
tqdm = "^4.67.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
